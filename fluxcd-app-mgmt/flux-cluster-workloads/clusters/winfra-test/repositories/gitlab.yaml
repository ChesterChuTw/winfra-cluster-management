---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gitlab-repo
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./repositories/gitlab/base
  # path: ./repositories/gitlab/overlays/winfra-test ## use overlay if need to override base
  prune: true
  force: false
  sourceRef:
    kind: GitRepository
    name: flux-workloads
