---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy-traces
  namespace: observability
spec:
  interval: 10m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: alloy
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: alloy-traces-helm-values
      valuesKey: values-alloy-traces.yaml
    - kind: ConfigMap
      name: alloy-traces-helm-values-override
      valuesKey: values-alloy-traces.yaml
      optional: true
    - kind: Secret
      name: alloy-traces-helm-sensitive-values
      valuesKey: values-alloy-traces.secrets.yaml
      optional: true