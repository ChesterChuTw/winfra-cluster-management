---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik
generatorOptions:
  disableNameSuffixHash: false
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: helm-values
    files:
      - values.yaml
configurations:
  - kustomizeconfig.yaml
