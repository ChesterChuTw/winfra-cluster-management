---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: calico-repo
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./repositories/calico/base
  # path: ./repositories/calico/overlays/winfra-prod ## use overlay if need to override base
  prune: true
  force: false
  sourceRef:
    kind: GitRepository
    name: flux-infrastructure
